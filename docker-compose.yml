version: '3.8'

services:
  soapbuy-bot:
    build: .
    container_name: soapbuy-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - TZ=UTC
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
